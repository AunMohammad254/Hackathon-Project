{
    "name": "ai-clinic-management",
    "version": "1.0.0",
    "description": "AI Clinic Management SaaS",
    "scripts": {
        "dev": "node dev.js",
        "build": "node build.js",
        "build:backend": "bun --cwd backend run build",
        "build:frontend": "bun --cwd frontend run build",
        "test": "bun --cwd backend test",
        "test:watch": "bun --cwd backend test --watch"
    },
    "devDependencies": {
        "@tailwindcss/postcss": "^4.2.1",
        "concurrently": "^9.2.1",
        "tailwindcss": "^4.2.1"
    },
    "dependencies": {
        "tw-animate-css": "^1.4.0"
    }
}